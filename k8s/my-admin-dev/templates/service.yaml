apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-admin-dev.fullname" . }}
  labels:
    {{- include "my-admin-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-admin-dev.selectorLabels" . | nindent 4 }}
