apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-project-dev.fullname" . }}
  labels:
    {{- include "my-project-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-project-dev.selectorLabels" . | nindent 4 }}
