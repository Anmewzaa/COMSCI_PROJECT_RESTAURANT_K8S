apiVersion: v1
kind: Service
metadata:
  name: {{ include "comsci-project.fullname" . }}
  labels:
    {{- include "comsci-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "comsci-project.selectorLabels" . | nindent 4 }}
